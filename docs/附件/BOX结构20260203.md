
```dataview
TABLE file.folder
FROM "BOX"
WHERE file.folder != "BOX/A-题/a0-真题库" 
  AND file.folder != "BOX/排除2" 
  AND file.folder != "BOX/排除3" 
  AND file.folder != "BOX/排除4"
SORT file.folder ASC, file.name ASC
```